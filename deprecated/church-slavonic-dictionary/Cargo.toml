[package]
name = "church-slavonic-dictionary"
version = "0.2.1"
edition = "2024"
rust-version = "1.85"
license = "MIT OR Apache-2.0"
repository = "https://github.com/gold-silver-copper/church-slavonic"
description = "DEPRECATED: superseded by the church-slavonic crate (0.4). It held Old Church Slavonic senses and lemmatisation over the rule-first facade; senses are now deterministic _n keys in the church-slavonic tables."
readme = "README.md"
keywords = ["church-slavonic", "deprecated"]
categories = ["text-processing"]
publish = true

[badges]
maintenance = { status = "deprecated" }

[dependencies]

# Standalone: not a member of the repository workspace.
[workspace]
