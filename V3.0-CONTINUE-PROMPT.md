# V3.0, continued: finish Part 1, close 3.0.0, then the twins

The prompt for the session after 9a2963e. `V3.0-PROMPT.md` is the plan
and stays the plan; this file says where it stopped and exactly what the
next steps are, in order, so no step is re-derived. Read first, in this
order: `V3.0-PROMPT.md`'s postscript, `HANDOFF-PROMPT.md`, the "V3.0
Part 0" and "V3.0 Part 1" entries of `CHANGELOG.md` and `NOTES.md`. Each
step ends with its gate measured, a CHANGELOG row under "3.0.0 (in
progress)", a dated NOTES.md entry (decisions and findings, what was
rejected and why), and a commit; push after each commit.

## Where it stopped

Commits b573263 (Part 0), b8370ac and 9a2963e (Part 1 steps 1–2). Stress
exception lists 1,871 → 1,344 → 1,083 (nouns 219, adjectives 211, verbs
632, pronouns 21); 43 named paradigms; the -надесѧть numerals as
`stems=tail=на́десѧть` with no overrides; the stem place through a
derivation is a crate rule (`stress::resolve_in` over `Vowels`); the
treebank rebuilt at zero mismatches (one cell 204,710, tagger 185,206,
several lexemes 13,027, verbatim 48,977); held-out recall unchanged
(95.48 / 89.31 / 90.89 / 99.25 / 98.07); tests, clippy and the game
green. Version still 2.3.0, no `v3.0.0` tag.

## The environment, before any command

- `export RUSTC_WRAPPER=` (empty) in every shell that runs cargo in this
  checkout: the shared sccache daemon was started by another project
  without Desktop access and the build fails with EPERM on
  `target/release/deps/*.d`. Do not stop or kill the daemon; another
  session's build uses it.
- Lexicon files are `include_str!`-embedded: `cargo build --release`
  before any `xtask` sees an edit.
- After any lexicon change: `cargo xtask fix-hand-alts`, then
  `cargo xtask build-treebank` (about 70 s), and read its last line —
  the stored treebank goes stale before it goes wrong (a set leaf whose
  cells no longer print alike renders through its first cell).
  `check-treebank` is the assertion on a rebuilt treebank, not the
  rebuild.
- A paradigm that changes an unattested cell (a whole block on `P`)
  enters only through `cargo xtask import polyakov --pos <pos> --write`;
  `refit-stress` refuses it by design. Never re-import the pronouns
  through Polyakov; `refit-stress --pos pron --write` or a hand edit
  with `H:` in `src`.
- The game (`~/Desktop/code/vertograd`): `cargo test --release` and
  `./scripts/headless-test.sh` after every lexicon change that a
  release will ship.

## Step 1 — Part 1 step 3: the 61 `adv-of=` adverbs

The Part 0 census (`cargo xtask census stress`, its last section)
classified them: 33 already printed by the adjective's own `adv` cell,
12 stressed on the adjective's stem where the adjective is end-stressed
(бла́гѡ ~ благі́й), 6 on the ending where the adjective is stem-stressed
(быстрѡ̀ ~ бы́стрый), 8 on none of the places (ве́селѡ ~ весе́лый), 2
unaccented.

1. The rule that decides an adverb line lives in the closed importer
   (`import/polyakov.rs`, the 2.2 rule that deleted 1,123 lines): an
   adverb an adjective in the lexicon prints exactly is not a line, its
   count goes to the adjective's note (`adv P:n`). The 33 fall to that
   rule now because the 3.0 re-imports moved the adjectives' stress; the
   importer must re-derive the decision against the current lexicon, so
   run `cargo xtask import polyakov --pos closed --write` and confirm the
   33 went and their counts are on the adjectives' notes. If the closed
   importer reads the adjectives from a stale snapshot rather than the
   built lexicon, fix that first.
2. The 12 and the 6 are an `adv` key on the adjective's stress column —
   `Cell::parse(Adjective, "adv")` already yields `Cell::Adv`, so `adv=S`
   / `adv=E` parse today. Name the two shapes in `lexicon/stress.tsv`
   from the census (`b.adv` = `E;adv=S`, exemplar благі́й; `a.adv` =
   `S;adv=E`, exemplar бы́стрый), with the counts. For the fitter to
   choose them, the adjective's attested cells must include the adverb:
   in the adjective importer, where Polyakov's adverb entries are
   matched to an adjective (`adverb_entries`, the `adv P:n` note), enter
   the attested adverb print as the `adv` cell's attested form so
   `fit::stress_column` sees it. Re-import adjectives, then closed
   (step 1's rule deletes the lines the adjective now prints). Where
   the print differs by a letter and not the accent, the line stays with
   `adv-of=` as before.
3. The 8 on none of the places and the 2 unaccented stay, with the
   census's classification in the line's note.
4. Gate: the deleted count (expect 33 + up to 18); `cargo xtask
   build-treebank`'s table — closed-class share not lower than 28.0%
   and several lexemes not above 2.1% (an adverb cell must not create a
   homonymy against a surviving closed line: the `adv` cell and the line
   never coexist for one print); `narrow-hand` still 0; the adjective
   reproduction number recorded (it may rise by the adverbs); the game's
   adverb strings, if any, unchanged.

## Step 2 — Part 1 step 4: the residue, recorded

1. `cargo xtask census stress` after step 1: the counts and the
   commonest shapes per part of speech. The verb residue is 632 lists in
   475 shapes; its commonest are the fitter's own one-number moves
   written as lists (`b{sg=S}` 13, `b{pl=S}` 8, `a{sg=E}` 7 — decide:
   either name them `b.sg`, `b.pl`, `a.sg` so they count as paradigms,
   or state in DESIGN that a one-number move is a paradigm the notation
   spells inline and stop counting them as lists; the second is the
   honest one — say so and change the census to count them apart), the
   past passive participle's retraction (`a{part.past.pass.short.m.sg.nom
   =<n>}` 10, ~3 shapes on `P`: decide whether `part.past.pass` on `P` is
   a shape the census shows at ≥ 3 and name it, or record it), and the
   proper names' citation cell (`a{nom.sg=<n>}` 20 nouns: a source fact,
   residue).
2. The 9 pronoun stress twins the arbiter never saw (`census forms`:
   ѻ҆́нꙋ 67:16, ѻ҆̑ны 19:0 on онъ.pron.2; наѳана́илъ 8:5, і҆а́ковъ 12:1 are
   citation-cell twins of names and stay): decide the two pronoun cells
   by hand in `lexicon/syn/pronouns.tsv` (the Bible's form as the
   primary, the other a variant with its count; `H:` added to `src`),
   rebuild, and record. Never a Polyakov pronoun re-import.
3. A paragraph in `docs/OPEN-DESIGNS.md` 5 saying what the residue is
   (Polyakov's own variation, a source's wrong tag, the names' citation
   cell, a genuine per-lexeme accent), with the final counts, so no
   later session re-opens it without a new source.
4. Gate: the counts on record; zero mismatches; the consistency test.

## Step 3 — Part 2: the close of 3.0.0

In this order, nothing skipped:

1. `cargo xtask eval`, `build-treebank`, `score-disambiguation` on the
   final lexicon. README: a 3.0 column in the held-out table (expected
   unchanged) and in the Bible-coverage table; the sizes table (the
   closed count after step 1); the line-format section: `P`, the stem
   place through a derivation, `tail=`; the stress inventory count.
2. CHANGELOG: "3.0.0 (in progress)" → "3.0.0 — 2026-09-…"; a release
   table at the head (lists 1,871 → final; paradigms 31 → final;
   reproduction; recall; coverage; the numerals; the adverbs); one
   sentence on why it is a major release (every Synodal stress column
   reads differently; `Analysis`/`Reading` gained `weight`; `Letters`
   and `resolve` changed shape).
3. `docs/DESIGN.md` re-read for the 3.0 paragraphs (stress column,
   stems `tail=`, weights, the arbiter); `docs/OPEN-DESIGNS.md` 5's
   outcome with the final numbers; `HANDOFF-PROMPT.md` rewritten for
   3.0.0 (what exists, the numbers, the open list = Parts 3–6 of
   `V3.0-PROMPT.md`); NOTES close entry; `V2.2-PROMPT.md`'s last
   postscript closed in one line pointing at `V3.0-PROMPT.md`;
   `V3.0-PROMPT.md`'s postscript updated.
4. Version 3.0.0 in `crates/church-slavonic/Cargo.toml` and the
   workspace's `church-slavonic` dependency version (still `2.0.0-dev`);
   the tagger and tools crates stay 0.1.0. `cargo test --release
   --workspace`, `cargo clippy --release --workspace` at zero warnings,
   the game's tests and headless run. Commit, tag `v3.0.0`, push with
   tags. Publishing to crates.io is the user's call: say whether the
   published crate is behind the tag, do not publish.

## Step 4 — Parts 3–6 of `V3.0-PROMPT.md`

Continue there, in order (the twins, the gold by register for 3.1.0;
the clause and calibration, the placement for 3.2.0). One thing the
Part 0 census settled for Part 3: гдⷭ҇ь and kin (5,767 tokens) are
`:amb` through the titlo index — господь.n (`b{…}`, Polyakov +
ru.wiktionary) and господь.n.2 (`a`, Polyakov's second sense, nom.pl
госпо́дїе) — so the rule of identity must handle a pair that differs in
its stress column because one side was fitted to fewer cells; and
ꙗзыкъ.n / .n.2 differ in animacy, which is grammar and stays two lines.
Read the twins list (`cargo xtask census homonymy`, its last two
sections) before writing the rule.

## What not to do

- Do not invent a paradigm the census does not show at ≥ 3 lines; do
  not keep a rule or paradigm that removes nothing.
- Do not change an id (the game persists them); grep the game's
  `slavonic.rs` before any cleaning.
- Do not re-import the pronouns through Polyakov; do not run
  `check-treebank` on a stale treebank and call the failure a bug.
- Do not edit a class tsv or a file under `treebank/` by hand; the hand
  overlay is `data/treebank-hand/`, the inventory `lexicon/stress.tsv`
  and the pronouns file are the hand-editable ones (with `H:`).
- Do not close 3.0.0 with a number missing from the README tables, and
  do not publish to crates.io without the user's word.
